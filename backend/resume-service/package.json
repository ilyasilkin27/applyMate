{
  "name": "resume-service",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "build": "mkdir -p build && bun build src/index.ts --outdir build"
  },
  "dependencies": {
    "@elysiajs/cookie": "^0.7.0",
    "@elysiajs/cors": "^0.7.0",
    "dotenv": "^16.4.7",
    "elysia": "^0.7.0",
    "ky": "^0.33.3"
  },
  "devDependencies": {
    "bun-types": "^1.0.0"
  }
}
